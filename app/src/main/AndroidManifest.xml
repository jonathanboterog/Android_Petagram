<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ingenico.petagram">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.ingenico.petagram.PetHistorical"
            android:parentActivityName="com.ingenico.petagram.MainActivity">
            <!--support for version below 4.0-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ingenico.petagram.MainActivity">
            </meta-data>
        </activity>
        <activity android:name="com.ingenico.petagram.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>